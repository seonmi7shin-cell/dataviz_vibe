# 핀테크 결제 데이터 분석 규칙

## 정제 순서 (가장 먼저 지킬 것)

「정제」, 「빈 값 정리」, 「깨끗한 파일」이라는 말이 나오면 **아래 세 단계를 이 번호 순서 그대로** 수행합니다.
순서를 바꾸거나 단계를 앞당기면 남는 행 수가 달라집니다. **다른 순서로 처리하지 마십시오.**

1. **범주 컬럼 결측 채우기** — 결제수단·지역은 `알수없음`, 연령대는 `미상`. 행을 지우지 않습니다.
2. **거래일시·거래금액 결측 행 삭제** — 금액이 없으면 합계에 넣을 수 없고 날짜가 없으면 월별로 묶을 수 없습니다.
3. **완전 중복 제거** — 모든 컬럼이 같은 행을 첫 건만 남깁니다. **반드시 마지막입니다.**

- 결과는 `data/핀테크_정제완료.csv` 로 저장합니다(`encoding="utf-8-sig"`). output 폴더가 아닙니다.
- 이 순서로 하면 12,150행이 **11,713행**이 됩니다. 다른 수가 나오면 순서가 어긋난 것입니다.
- **앞뒤 공백 제거(`.str.strip()`)는 정제 단계가 아닙니다.** 집계 직전에 합니다. 1번보다 먼저 하면
  중복·결측 건수가 원본과 달라져 11,738행처럼 다른 값이 나옵니다.
- 음수와 0원 이하 거래는 정제 단계에서 건드리지 않습니다. 이상치 처리는 따로 지시할 때 합니다.

## 데이터
- 원본은 data 폴더, 산출물은 output 폴더
- 분석 대상 파일은 지시할 때 @로 지정
- 원본 파일은 수정하지 않음

## 코드 작성 규칙

### 실행 위치와 무관하게 동작할 것
스크립트 맨 위에 아래 한 줄을 넣습니다. 이 줄이 없으면 다른 폴더에서 실행할 때
`data/...` 를 찾지 못해 FileNotFoundError 가 납니다.

```python
import os
os.chdir(os.path.dirname(os.path.abspath(__file__)))   # 스크립트가 scripts 폴더에 있으면 ".." 를 붙여 상위로
```

경로는 `os.path.join` 으로 조립하고, 문자열에 `\` 를 직접 쓰지 않습니다.
출력 폴더는 `os.makedirs(..., exist_ok=True)` 로 없으면 만들고 씁니다.

### 주석을 상세히 달 것
비개발자가 읽고 이해할 수 있어야 합니다. 코드를 한국어로 옮겨 적는 주석이 아니라
**왜 그렇게 쓰는지, 빼면 어떻게 되는지**를 적습니다.

```python
# 쓰지 않음:  # 업종별로 묶어서 합계
# 씀:         # 업종별로 묶어 합계를 냄. sort_values 로 큰 값부터 정렬해야 막대가 내림차순으로 보임
```

- 파일 맨 위에 이 스크립트가 무엇을 하는지 서너 줄로 설명합니다.
- 처음 나오는 함수·인자에는 무슨 역할인지 한 줄씩 답니다.
- 숫자 상수(1.5, 0.4 등)에는 그 값을 쓴 이유를 적습니다.

### 차트
```python
plt.rcParams["font.family"] = "Malgun Gothic"   # 한글이 네모로 깨지지 않게
plt.rcParams["axes.unicode_minus"] = False      # 마이너스 기호 깨짐 방지
```

### 결과 확인
실행하면 터미널에 무엇이 만들어졌는지 요약이 찍혀야 합니다. 행 수, 집계 결과, 저장 경로.

### 대시보드는 Streamlit 으로 만들고 실행은 사람이 합니다
「대시보드」라고 하면 **Streamlit 앱**(`streamlit run` 으로 띄우는 파이썬 파일)을 뜻합니다.
단일 HTML 파일로 만들지 않습니다. 필터가 값을 바꾸면 화면이 다시 그려지는 구조가 이 과정의 학습 내용입니다.

**`streamlit run` 을 직접 실행하지 마십시오.** 서버가 계속 떠 있어 다음 지시를 받을 수 없습니다.
파일을 만든 뒤 실행할 명령만 알려 주고 멈춥니다.

```
streamlit run scripts/대시보드.py
```

## 분석 규칙
- 거래금액은 원 단위 정수로 표기
- 비율은 백분율 소수점 둘째 자리
- 집계 단위는 가맹점업종 · 지역 · 결제수단 · 연월
- 결측이 있어도 그 컬럼을 분석에서 빼지 말고 건수를 함께 보고

## 정제 규칙

맨 위 「정제 순서」 절을 따릅니다. 단계마다 몇 행이 줄었는지 출력합니다.

## 저장 규칙
- 표는 output 폴더에 CSV (encoding="utf-8-sig")
- 차트는 output/charts 폴더에 PNG
- 코드는 scripts 폴더

### 차트는 저장과 동시에 창으로도 띄울 것
차트를 그리면 `output/charts` 에 PNG로 저장하고, **같은 실행에서 화면에 창으로도 띄운다.**
`plt.savefig()` 로 저장한 뒤 이어서 `plt.show()` 를 호출한다 (savefig 가 먼저, show 는 그 다음 —
순서를 바꾸면 show 이후 캔버스가 초기화되어 빈 파일이 저장될 수 있다).

## 이력 관리

**커밋은 사람이 시킬 때만 합니다.** 코드를 만들거나 고쳤다고 알아서 커밋하지 마십시오.
무엇을 언제 남길지는 사람이 정합니다. 시키지 않았는데 올라가면 되돌리는 일이 더 번거롭습니다.

**커밋하라고 하면 GitHub 에 올리는 것까지 한 묶음으로 합니다.** `git add` · `git commit` · `git push`
세 가지를 한 번에 수행하고, 따로 「올려 줘」라고 다시 말하지 않아도 됩니다.

- 커밋 메시지는 무엇을 만들었는지 한국어 한 줄
- output 폴더도 커밋 대상입니다 (더 이상 .gitignore 제외 아님)

저장소 주소는 여기 적지 않습니다. `git remote add origin` 을 한 번 해 두면
git 이 기억하므로 `git push` 만으로 올라갑니다.

## 보고 규칙
- 실행해 얻은 값만 보고
- 파일에 없는 수치를 만들지 않음
